import dashboard from './dashboard';

// ==============================|| MENU ITEMS ||============================== //
// Classic UI stays at apps/frontend. This menu is for frontend-v2 only.

const menuItems = {
  items: [dashboard]
};

export default menuItems;
