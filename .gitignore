# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
*.egg-info/
dist/
build/
.pytest_cache/

# Environment
.env
*.env.local

# Node
node_modules/
dist/
.vite/

# Uploads / generated
uploads/
reports/
*.apk

# IDE
.vscode/
.idea/
*.swp

# Docker
*.log

# ML models (large binary files — use Git LFS or store separately)
*.pkl
*.joblib
*.xgb
# Keep .json model files out too (xgb model is 5.9 MB — fine, but blobs are huge)
# services/risk-scoring/models/*.json  ← intentionally kept (small, needed for Docker build)

# Ollama model data & Docker volume data — NEVER commit these (can be GBs)
infra/data/
infra/volumes/
infra/postgres_data/
infra/minio_data/
infra/chroma_data/

# DB
*.db
*.sqlite

# OS
.DS_Store
Thumbs.db

# PDF submission
boi_submission.pdf
boi-submisson.pdf
datasets/raw